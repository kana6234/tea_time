<%= render 'public/items/search_form' %>
<%= @items.count %>件
<% @item.each do |item| %>
  <%= link_to item_path(item.id) do %>
    <%= render 'public/posts/list', post: item.post %>
  <% end %>
<% end %>
<%= paginate @item %>